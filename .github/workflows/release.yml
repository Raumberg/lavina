name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
            suffix: linux-x86_64
          - os: macos-latest
            arch: arm64
            suffix: macos-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install g++ (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-14
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100

      - name: Bootstrap
        run: make bootstrap

      - name: Test
        run: make test

      - name: Build
        run: make build && make lvpkg

      - name: Package
        run: |
          mkdir -p dist
          tar czf dist/lavina-${{ matrix.suffix }}.tar.gz -C build lavina lvpkg

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lavina-${{ matrix.suffix }}
          path: dist/lavina-${{ matrix.suffix }}.tar.gz

  build-windows:
    runs-on: windows-latest
    continue-on-error: true
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v4

      - uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          install: mingw-w64-ucrt-x86_64-gcc make diffutils zip

      - name: Bootstrap
        run: make bootstrap

      - name: Test
        run: make test

      - name: Build
        run: make build && make lvpkg

      - name: Package
        run: |
          mkdir -p dist
          cd build && zip ../dist/lavina-windows-x86_64.zip lavina.exe lvpkg.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lavina-windows-x86_64
          path: dist/lavina-windows-x86_64.zip

  release:
    needs: [build, build-windows]
    if: always() && needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
