// Test: extern import path — verifies -I flag is passed to g++
extern "test_ext_lib.h" import "tests/ext":
    int32 fn ext_add(int32 a, int32 b)
    int32 fn ext_mul(int32 a, int32 b)

void fn main():
    int32 a = 3
    int32 b = 4

    int32 sum = ext_add(a, b)
    lv_assert(sum == 7, "ext_add(3, 4) should be 7")

    int32 product = ext_mul(a, b)
    lv_assert(product == 12, "ext_mul(3, 4) should be 12")
